apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: shop-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/adisoncavani/shop-microservices/notification-service:0.0.7
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: env-shared
            - secretRef:
                name: shared-secrets
            - secretRef:
                name: notification-service
          env:
            - name: OTEL_SERVICE_NAME
              value: notification-service
            - name: OTEL_RESOURCE_ATTRIBUTES
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: shop-prod
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
