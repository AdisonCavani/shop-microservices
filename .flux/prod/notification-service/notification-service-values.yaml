apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: shop-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/adisoncavani/shop-microservices/notification-service:0.0.5
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: notification-service
          env:
            - name: Kestrel__EndpointDefaults__Protocols
              value: "Http2"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://aspire-dashboard:18889"
            - name: OTEL_SERVICE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: shop-prod
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
