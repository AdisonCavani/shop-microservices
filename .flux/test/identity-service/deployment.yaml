apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: test-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: ghcr.io/adisoncavani/shop-microservices/gateway:0.0.8
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: env-shared
            - secretRef:
                name: shared-secrets
            - secretRef:
                name: identity-service
          env:
            - name: OTEL_SERVICE_NAME
              value: identity-service
            - name: OTEL_RESOURCE_ATTRIBUTES
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
